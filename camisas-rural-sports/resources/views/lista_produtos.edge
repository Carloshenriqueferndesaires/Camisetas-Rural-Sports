<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lista de Produtos - Rural Sports</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap" rel="stylesheet">

  <style>
    body {
      background-color: #0f0c25;
      color: #fff;
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 40px;
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
      color: aquamarine;
    }

    .container {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
      gap: 20px;
      max-width: 1000px;
      margin: 0 auto;
    }

    .product {
      background-color: #14122b;
      padding: 20px;
      border-radius: 15px;
      text-align: center;
      box-shadow: 0 0 10px rgba(0, 255, 191, 0.1);
      transition: 0.3s;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      min-height: 460px;
    }

    .product img {
      width: 100%;
      height: 250px;
      object-fit: cover;
      display: block;
      margin: 0 auto 15px auto;
      border-radius: 12px;
    }

    .product:hover {
      transform: scale(1.03);
      box-shadow: 0 0 15px rgba(0, 255, 191, 0.3);
    }

    .product h3 {
      margin: 10px 0 5px;
      font-size: 18px;
      color: #fff;
    }

    .product p {
      color: #b6b6b6;
      font-size: 14px;
    }

    .product span {
      display: block;
      margin-top: 8px;
      font-weight: bold;
      color: aquamarine;
    }

   .btn {
  background-color: #000; /* preto igual ao Compre Agora */
  color: #fff;
  font-size: 15px;
  padding: 12px 40px;
  font-weight: 600;
  border: none;
  border-radius: 50px;
  border-top-left-radius: 0; /* mesmo detalhe do botão original */
  transition: background 0.3s ease, transform 0.2s ease;
  text-decoration: none;
  display: inline-block;
  margin: 5px;
  cursor: pointer;
}

.btn:hover {
  background-color: #2c7a7b; /* verde água igual ao Compre Agora */
  transform: scale(1.05);
  cursor: pointer;
}


    .btn-delete {
      background-color: #ff4d4d;
      color: #fff;
    }

    .btn-delete:hover {
      background-color: #e64545;
    }

    .center {
      text-align: center;
      margin-top: 40px;
    }

    .top-buttons {
      text-align: center;
      margin-bottom: 30px;
    }
  </style>
</head>
<body>
  <h1>Produtos Cadastrados</h1>


  <div class="top-buttons">
    <div class="center">
  <a href="/produtos/novo" class="btn">Cadastrar Novo Produto</a>
  <a href="/" class="btn">Voltar</a>
  
</div>


  <div class="container">
    @each(product in products)
      <div class="product">
        @if(product.imageUrl)
          <img src="{{ product.imageUrl }}" alt="Imagem do produto">
        @endif

        <h3>{{ product.name }}</h3>
        <p>{{ product.description }}</p>
        <span>R$ {{ product.price }}</span>

        <form action="/produtos/{{ product.id }}/editar" method="GET">
          <button type="submit" class="btn">Editar</button>
        </form>

        <form action="/produtos/{{ product.id }}/deletar" method="POST">
          <button type="submit" class="btn btn-delete">Excluir</button>
        </form>
      </div>
    @endeach
  </div>
</body>
</html>
